{
  "name": "ridar",
  "version": "0.1.0",
  "private": true,
  "description": "Commercial awareness briefings on an interactive globe",
  "scripts": {
    "news": "node fetch-news.js",
    "dry": "node run.js --dry",
    "build": "node run.js",
    "build:force": "node run.js --force",
    "test:uk": "node run.js --only GBR --force"
  },
  "dependencies": {
    "rss-parser": "^3.13.0"
  },
  "engines": {
    "node": ">=18"
  }
}
